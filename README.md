# Badge Beadgrid

Badge Beadgrid is a Vite + React SPA that turns Shields.io SVG badge URLs into fuse bead patterns. Badge fetching, palette matching, previewing, and PNG/SVG export all run in the browser.

## Development

```sh
npm install
npm run dev
```

## Cloudflare Pages

```sh
npm run deploy
```

The Pages output directory and project name are configured in `wrangler.jsonc` and the deploy script.

## License

MIT
